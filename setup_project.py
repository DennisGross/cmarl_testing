import os

# Create the 'logs' and 'plots' directories
os.makedirs('logs', exist_ok=True)
os.makedirs('plots', exist_ok=True)
# Create central_policies
os.makedirs('central_policies', exist_ok=True)
os.makedirs('decentral_policies', exist_ok=True)